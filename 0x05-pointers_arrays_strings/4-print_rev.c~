#include "main.h"

/**
 *print_rev - Prints a strint in reverse
 *
 *@s: String to be printed
 *Return: void
 */
void print_rev(char *s)
{
	int len;

	len = sizeof(*s);

	while (len >= 0)
	{
		_putchar(*(s + len));
		len--;
	}
	_putchar('\n');
}
