#include "main.h"

/**
 *rot13 - Rotates the alphabet by 13 places
 *
 *@a: Strings
 *Return: An array of characters
 */
char *rot13(char *a)
{
	int i, j;

	i = 0;
	while (a[i] != '\0')
	{
		if (isalpha(a[i]))
		{
			
		}
	}
	return (a);
}
